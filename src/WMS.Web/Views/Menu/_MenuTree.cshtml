@model List<WMS.Application.DTOs.MenuDto>

<ul class="menu-tree-list">
    @foreach (var menu in Model)
    {
        <li class="menu-tree-item">
            <div class="menu-item-card">
                <div class="menu-item-header">
                    <div class="menu-item-info">
                        <span class="menu-item-icon">@(menu.Icon ?? "📁")</span>
                        <div class="menu-item-details">
                            <span class="menu-item-name">@menu.Name</span>
                            <span class="code-badge">@menu.Code</span>
                        </div>
                    </div>
                    <div class="menu-item-meta">
                        <span class="menu-type-badge @(menu.MenuType == 0 ? "badge-internal" : "badge-external")">
                            @(menu.MenuType == 0 ? "内部" : "外部")
                        </span>
                        @if (menu.IsEnabled)
                        {
                            <span class="badge-status badge-success">
                                <span class="badge-dot"></span>
                                启用
                            </span>
                        }
                        else
                        {
                            <span class="badge-status badge-danger">
                                <span class="badge-dot"></span>
                                禁用
                            </span>
                        }
                    </div>
                    <div class="menu-item-actions">
                        <a href="@Url.Action("Create", "Menu", new { parentId = menu.Id })" class="btn-action btn-add-child" title="添加子菜单" data-permission="Menu:Add" data-permission-action="hide">
                            <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" fill="currentColor" viewBox="0 0 16 16">
                                <path d="M8 4a.5.5 0 0 1 .5.5v3h3a.5.5 0 0 1 0 1h-3v3a.5.5 0 0 1-1 0v-3h-3a.5.5 0 0 1 0-1h3v-3A.5.5 0 0 1 8 4"/>
                            </svg>
                            子菜单
                        </a>
                        <a href="@Url.Action("Edit", "Menu", new { id = menu.Id })" class="btn-action btn-edit" title="编辑" data-permission="Menu:Edit" data-permission-action="hide">
                            <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" fill="currentColor" viewBox="0 0 16 16">
                                <path d="M12.854.146a.5.5 0 0 0-.707 0L10.5 1.793 14.207 5.5l1.647-1.646a.5.5 0 0 0 0-.708zm.646 6.061L9.793 2.5 3.293 9H3.5a.5.5 0 0 1 .5.5v.5h.5a.5.5 0 0 1 .5.5v.5h.5a.5.5 0 0 1 .5.5v.5h.5a.5.5 0 0 1 .5.5v.207l6.5-6.5zm-7.468 7.468A.5.5 0 0 1 6 13.5V13h-.5a.5.5 0 0 1-.5-.5V12h-.5a.5.5 0 0 1-.5-.5V11h-.5a.5.5 0 0 1-.5-.5V10h-.5a.5.5 0 0 1-.175-.032l-.179.178a.5.5 0 0 0-.11.168l-2 5a.5.5 0 0 0 .65.65l5-2a.5.5 0 0 0 .168-.11l.178-.178z"/>
                            </svg>
                            编辑
                        </a>
                        <button class="btn-action btn-delete" onclick="deleteMenu(@menu.Id)" title="删除" data-permission="Menu:Delete" data-permission-action="hide">
                            <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" fill="currentColor" viewBox="0 0 16 16">
                                <path d="M5.5 5.5A.5.5 0 0 1 6 6v6a.5.5 0 0 1-1 0V6a.5.5 0 0 1 .5-.5m2.5 0a.5.5 0 0 1 .5.5v6a.5.5 0 0 1-1 0V6a.5.5 0 0 1 .5-.5m3 .5a.5.5 0 0 0-1 0v6a.5.5 0 0 0 1 0z"/>
                                <path d="M14.5 3a1 1 0 0 1-1 1H13v9a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V4h-.5a1 1 0 0 1-1-1V2a1 1 0 0 1 1-1H6a1 1 0 0 1 1-1h2a1 1 0 0 1 1 1h3.5a1 1 0 0 1 1 1zM4.118 4 4 4.059V13a1 1 0 0 0 1 1h6a1 1 0 0 0 1-1V4.059L11.882 4zM2.5 3h11V2h-11z"/>
                            </svg>
                            删除
                        </button>
                    </div>
                </div>
                @if (menu.Buttons.Any())
                {
                    <div class="menu-buttons-section">
                        <span class="buttons-label">按钮：</span>
                        <div class="buttons-list">
                            @foreach (var button in menu.Buttons)
                            {
                                <span class="button-tag">@button.Name</span>
                            }
                        </div>
                    </div>
                }
            </div>
            @if (menu.Children.Any())
            {
                <div class="menu-children-wrapper">
                    @await Html.PartialAsync("_MenuTree", menu.Children)
                </div>
            }
        </li>
    }
</ul>

